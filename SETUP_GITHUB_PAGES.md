# 🚀 GitHub Pages 快速設定

## ⚠️ 重要：需要在 GitHub 網站上設定

部署已完成，但需要在 GitHub 網站上啟用 Pages 功能。

---

## 📋 設定步驟（必須完成）

### 1. 前往 GitHub Repository

打開：**https://github.com/ke22/map_militrary-exercise**

### 2. 進入 Settings（設定）

點擊 Repository 頁面上方的 **Settings** 標籤

### 3. 找到 Pages 設定

在左側選單中，點擊 **Pages**

### 4. 設定 Source

在 **Source** 區塊：
- 選擇：**Deploy from a branch**
- **Branch**：選擇 `gh-pages`
- **Folder**：選擇 `/ (root)`
- 點擊 **Save**（儲存）

### 5. 等待部署

- GitHub 需要 **1-5 分鐘** 處理
- 頁面上方會顯示部署狀態
- 完成後會顯示綠色的勾勾 ✅

---

## 🔗 完成後的網址

設定完成後，應用可以在以下網址訪問：

**https://ke22.github.io/map_militrary-exercise/**

---

## ✅ 驗證步驟

1. 等待 1-5 分鐘
2. 訪問：https://ke22.github.io/map_militrary-exercise/
3. 如果看到地圖，表示成功！
4. 如果還是 404，檢查：
   - Settings → Pages → Source 是否選擇了 `gh-pages`
   - 是否已點擊 Save
   - 等待時間是否足夠

---

## 🔄 如果還是 404

### 檢查 gh-pages branch

確認 branch 存在且有檔案：

```bash
# 在本地執行
git fetch origin gh-pages
git checkout gh-pages
ls -la
# 應該看到 index.html 和 assets/ 目錄
```

### 重新部署

如果檔案不正確，重新部署：

```bash
git checkout main
npm run deploy
```

然後回到 GitHub 網站，確認 Settings → Pages 設定正確。

---

## 📞 需要幫助？

如果設定後還是 404，請檢查：
1. GitHub Pages 是否已啟用（Settings → Pages）
2. Source branch 是否選擇 `gh-pages`
3. 是否已等待足夠時間（1-5 分鐘）
4. 瀏覽器是否清除了快取

